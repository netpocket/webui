  <div class="panel-heading">
    <h3 class="panel-title"><%= name %></h3>
  </div>
  <div class="panel-body">
    <div class="list-group">
      <div class="list-group-item display">
        <span>Nothing to display</span>
      </div>
      <% for (var fname in features) { %>
        <div class="list-group-item feature">
          <span class="list-group-item-heading"><%= fname %></span>
          <ul class="">
            <% for (var action in features[fname] ) { %>
              <div>
                <a href="#" data-name="<%= fname %>:<%= action %>">
                  <%= action %>
                </a>
              </div>
            <% } %>
          </ul>
        </div>
      <% } %>
    </div>
  </div>
</div>

